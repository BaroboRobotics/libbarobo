int bachk(char *str) {
    void *fptr;
    int retval;

    fptr = dlsym(_Chbluetooth_handle, "bachk_chdl");
    if(fptr == NULL) {
        fprintf(_stderr, "Error: %s(): dlsym(): %s\n", __func__,  dlerror());
        return -1;
    }
    dlrunfun(fptr, &retval, bachk, str);
    return retval;
}
