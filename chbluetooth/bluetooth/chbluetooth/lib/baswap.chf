void baswap(bdaddr_t *dst, bdaddr_t *src) {
    void *fptr;

    fptr = dlsym(_Chbluetooth_handle, "baswap_chdl");
    if(fptr == NULL) {
        fprintf(_stderr, "Error: %s(): dlsym(): %s\n", __func__,  dlerror());
        return;
    }
    dlrunfun(fptr, NULL, baswap, dst, src);
}
