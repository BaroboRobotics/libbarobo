int basnprintf(char *str, size_t size, char *format, ...) {
    void *fptr;
    va_list ap_ch;
    int retval;

    va_start(ap_ch, format);
    fptr = dlsym(_Chbluetooth_handle, "basnprintf_chdl");
    if(fptr == NULL) {
        fprintf(_stderr, "Error: %s(): dlsym(): %s\n", __func__,  dlerror());
        return -1;
    }
    dlrunfun(fptr, &retval, basnprintf, str, size, format, ap_ch);
    va_end(ap_ch);
    return retval;
}
