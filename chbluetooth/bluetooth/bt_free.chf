void bt_free(void *ptr) {
    void *fptr;

    fptr = dlsym(_Chbluetooth_handle, "bt_free_chdl");
    if(fptr == NULL) {
        fprintf(_stderr, "Error: %s(): dlsym(): %s\n", __func__,  dlerror());
        return;
    }
    dlrunfun(fptr, NULL, bt_free, ptr);
}
